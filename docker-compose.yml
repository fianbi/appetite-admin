services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORTA}:${PORTA}"
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    restart: unless-stopped